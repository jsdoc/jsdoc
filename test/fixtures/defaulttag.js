/**
    @default
 */
var request = null;

/**
    @default
 */
var response = 'ok';

/**
    @default
 */
var rcode = 200;

/**
    @default
 */
var rvalid = true;

/**
    @default
 */
var rerrored = false;

/**
    @default the parent window
 */
var win = getParentWindow();

/**
    @default
 */
var header = getHeaders(request);